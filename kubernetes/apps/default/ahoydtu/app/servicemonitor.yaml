apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    k8s-app: ahoydtu
  name: ahoydtu
spec:
  endpoints:
  - path: /metrics
    interval: 30s
    port: metrics
    scheme: http
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      k8s-app: ahoydtu