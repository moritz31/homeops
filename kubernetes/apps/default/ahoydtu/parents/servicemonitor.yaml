apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    k8s-app: ahoydtu-parents
  name: ahoydtu-parents
spec:
  endpoints:
  - path: /metrics
    interval: 30s
    port: metrics
    scheme: http
    relabelings:
      - sourceLabels: [__address__]
        targetLabel: __address__
        regex: (.*)
        replacement: "192.168.1.157"
        action: replace
  namespaceSelector:
    matchNames:
    - default
  selector:
    matchLabels:
      k8s-app: ahoydtu-parents